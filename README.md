# CLLFST voting app

```
# clone the repo
git clone https://github.com/cllfst/cllfst-vote.git && cd cllfst-vote

# start usgin docker
docker-compose up -d

# dev using docker
docker-compose up -d --build

```
